{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "compile-main-program",
            "command": "/usr/bin/make",
            "args": [],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "compiler: /usr/bin/g++"
        }
    ]
}